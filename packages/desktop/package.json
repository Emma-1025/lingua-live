{
  "name": "@lingua-live/desktop",
  "version": "0.3.1",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:smoke": "tauri build --no-bundle",
    "start": "tauri run --release"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.4.0"
  }
}
