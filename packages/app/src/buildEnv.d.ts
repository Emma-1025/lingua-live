/** Inlined at build time via Vite `define` (see vite.config.ts). */
declare const process: {
  env: {
    DEEPSEEK_API_KEY?: string;
    LINGUA_VENDOR_MODE?: string;
    LINGUA_MOCK_ASR_SCENARIO?: string;
  };
};
